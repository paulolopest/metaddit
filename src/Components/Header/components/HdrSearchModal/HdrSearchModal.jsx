import React, { useCallback } from 'react';
import './HdrSearchModal.scss';

const HdrSearchModal = ({ searchModal, setSearchModal }) => {
	return (
		<div className="hdr-src-mdl animeDown">
			<div>SearchModal</div>
		</div>
	);
};

export default HdrSearchModal;
